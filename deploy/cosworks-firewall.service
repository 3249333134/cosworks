[Unit]
Description=Restrict Cosworks database ports from the public interface
After=docker.service
Requires=docker.service

[Service]
Type=oneshot
ExecStart=/usr/local/sbin/cosworks-firewall
RemainAfterExit=yes

[Install]
WantedBy=multi-user.target
